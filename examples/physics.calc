# Базовые SI-единицы
UNIT Length m
UNIT Time s
UNIT Mass kg

# Производные через скорость и ускорение
9,8 (m / s ^ 2) gravity
10 s fallTime

# Свободное падение: v = g * t,  d = g * t^2 / 2
gravity * fallTime = velocity
gravity * fallTime ^ 2 / 2 = distance

# Сила (Ньютон): F = m * a
70 kg mass
mass * gravity = weight
